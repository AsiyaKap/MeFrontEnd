git config --list  выводит список настроек

git config --help все команды для гита 

https://github.com/AsiyaKap/
путь для проекта
// -для перехода
dir все папки в указаном пути 
../../file/js чтод дойти до файла в деректории выше
public/img/img.jpg путь к файлу
script.js   файл там где основное


чтоб перенести праект в депазеторий 
1 перейти в папку где хранится проект
 mkdir  создать папку

cd ./test
git init создание пустого репозетория 

git remote add origin https://github.com/AsiyaKap/MeFrontEnd
для связи с гитхабом 

скопировать репозиторий  на копьютер 
git clone https://github.com/AsiyaKap/MeFrontEnd

git clone https://github.com/AsiyaKap/MeFrontEnd mt test  создаем папку и копируем в нее из репозитория 

git status     -показывает какие изменения проишзошли в гите c прошлого захода

git log   проверяет изменения 

git diff  сравнивание файлов  еслли работаешь с кемто еще

git add .     доовляет  все файлы в очередь   которые не находятся в папке гит игнор . туда заносятся файлы которые не будут сливаться с остальными
git add "названип нужного файла" сливает только один файл

git comit -m "name"   - название как будет подписано то что вы заливаете 

git push origin master    -куда будет залито 

git pool   слить 


git commit -amend - убирает последний комит

git mv "название файла" "название второго файла"  меняет местами файлы


git rm -f "имя файла" принудительно удаляет файл из отселживания в гите когда мы сливаем в даный момент 

git rm -cached "имя файла"  удаляет файл из комита .. чтобы вернуть  git add "имя файла"  и он опять будет в комите

git remote -v   показывает репозитории и ссылки на них

git remote rename "старое имя" "новое имя" меняет имя репозитория но только локально












